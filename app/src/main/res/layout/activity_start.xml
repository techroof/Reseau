<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".StartActivity"
    android:padding="60dp">

    <TextView
        android:id="@+id/select_role_text"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Select Your Role"
        android:textSize="24sp"
        app:fontFamily="@font/inter_bold"
        android:gravity="center"
        android:textColor="@color/headingColorDark"
        android:layout_marginTop="100dp"
        app:layout_constraintTop_toTopOf="parent"/>

    <TextView
        android:id="@+id/select_role_text2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Please select your role so that you can use features accordingly"
        android:gravity="center"
        android:textColor="@color/lightTextColor"
        android:layout_marginTop="15dp"
        app:layout_constraintTop_toBottomOf="@id/select_role_text"/>

    <Button
        android:id="@+id/creator_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Creator"
        app:backgroundTint="@null"
        android:background="@drawable/btn_bg"
        android:textColor="@color/headingColorDark"
        app:fontFamily="@font/inter_bold"
        android:layout_marginStart="50dp"
        android:layout_marginEnd="50dp"
        app:layout_constraintVertical_bias="0.3"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintTop_toBottomOf="@id/select_role_text2"/>

    <Button
        android:id="@+id/supporter_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Supporter"
        app:backgroundTint="@null"
        android:background="@drawable/btn_bg"
        android:textColor="@color/headingColorDark"
        app:fontFamily="@font/inter_bold"
        android:layout_marginTop="50dp"
        android:layout_marginStart="50dp"
        android:layout_marginEnd="50dp"
        app:layout_constraintTop_toBottomOf="@id/creator_btn"/>

</androidx.constraintlayout.widget.ConstraintLayout>